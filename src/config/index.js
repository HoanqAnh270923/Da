import routes from "./routes";
import { cssPropertyComponents } from "./css.type";
import htmlAttributes from "./htmlAttribute";

const config = {
  routes,
  cssPropertyComponents,
  htmlAttributes,
};

export default config;
